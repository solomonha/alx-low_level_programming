#include "main.h"

/**
 * add - adds two integers
 * @a: is integers to add
 * @b: is integer to add
 *
 * Return: result
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
